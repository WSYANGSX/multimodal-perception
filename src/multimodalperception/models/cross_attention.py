from __future__ import annotations

import torch
import torch.nn as nn


class CorssAtten(nn.Module):
    def __init__(self, inputs: list[torch.Tensor]) -> None:
        pass

    def forward(self, *input):
        return super().forward(*input)
